const initialState = {
    associate: {
        name: 'associate'
    }
}

const swotReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default swotReducer;